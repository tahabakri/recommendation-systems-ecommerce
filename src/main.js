// Entry point. Vite bundles from here: the CSS import becomes the compiled
// Tailwind stylesheet, and initApp() boots the demo once the module loads.
import './styles.css';
import { initApp } from './app.js';

initApp();
